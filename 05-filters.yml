- name: filter
  hosts: localhost
  vars:
   CONTENT: "Fruit - Apple\nDrink - coke\nFood - Burger"
   tasks:
     - name: filter
       ansible.builtin.debug:
         msg: "{{ CONTENT | split('/n')}}"
